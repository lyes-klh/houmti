import React from 'react';
import { Box } from '@chakra-ui/react';

const PostContent = () => {
  return (
    <Box mb={2} px={4}>
      This is a post !
    </Box>
  );
};

export default PostContent;
